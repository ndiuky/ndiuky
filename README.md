# ndiuky kawaii web room

A Vue + TypeScript + Vite landing page with an anime gif background, soft glass panels, GitHub profile data, and contact cards.

## Scripts

- `bun run dev` starts local development.
- `bun run build` type-checks and builds the site.
- `bun run preview` serves the production build locally.

The background gif lives at `public/anime.gif`.
